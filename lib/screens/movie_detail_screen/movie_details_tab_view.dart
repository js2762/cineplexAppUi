import 'package:flutter/material.dart';

class MovieDetailsTabView extends StatelessWidget {
  const MovieDetailsTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Movie Details',
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
